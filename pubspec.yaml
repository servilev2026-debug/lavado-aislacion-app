name: lavado_aislacion
description: App de control de lavado de aislación - compilación en la nube
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  image_picker: ^1.0.7
  geolocator: ^13.0.4
  path_provider: ^2.1.3
  pdf: ^3.10.8
  share_plus: ^12.0.2
  intl: ^0.19.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
